-- Creates the database if it doesn't exist.
-- The Python initializer replaces {{DB_NAME}} with your configured DB name.
CREATE DATABASE IF NOT EXISTS `{{DB_NAME}}` CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
